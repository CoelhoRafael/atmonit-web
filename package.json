{
  "name": "monilog-atmonit-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node ./bin/www",
    "dev": "nodemon --exec node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "apt update && apt upgrade -y && apt-get install python3 -y && git clone https://github.com/m-wrzr/populartimes.git && pip3 install --upgrade setuptools && pip3 install ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mssql": "^7.1.3",
    "mysql2": "^2.2.5",
    "nodemon": "^2.0.7",
    "path": "^0.12.7",
    "python-shell": "^3.0.1",
    "toastify-js": "^1.11.2"
  }
}